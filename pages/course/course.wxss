/* pages/course/course.wxss */

.container {
  min-height: 100vh;
  background: var(--bg);
  padding-bottom: 120rpx; /* 为 tabBar 留出空间 */
}

.header {
  padding: 48rpx 32rpx 40rpx;
}

.header-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.header-left { flex: 1; }

.header-title {
  font-size: 40rpx;
  font-weight: 800;
  color: #fff;
  margin-bottom: 8rpx;
}

.header-sub {
  font-size: 26rpx;
  color: rgba(255,255,255,0.85);
}

.header-search-btn {
  width: 72rpx;
  height: 72rpx;
  background: rgba(255,255,255,0.2);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  color: #fff;
  flex-shrink: 0;
}

/* 搜索栏 */
.search-bar {
  background: #fff;
  padding: 16rpx 24rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.search-input {
  flex: 1;
  height: 70rpx;
  background: #f5f5f8;
  border-radius: 35rpx;
  padding: 0 28rpx;
  font-size: 28rpx;
  color: #333;
}

.search-clear {
  font-size: 36rpx;
  color: #999;
  width: 50rpx;
  text-align: center;
  flex-shrink: 0;
}

/* 筛选栏 */
.filter-bar {
  display: flex;
  background: #fff;
  padding: 0 24rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.filter-tab {
  flex: 1;
  text-align: center;
  padding: 24rpx 0;
  font-size: 28rpx;
  color: var(--text-sub);
  position: relative;
}

.filter-tab.active {
  color: var(--primary);
  font-weight: 600;
}

.filter-tab.active::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 40rpx;
  height: 4rpx;
  background: var(--primary);
  border-radius: 100rpx;
}

/* 分类标签 */
.cat-scroll {
  background: #fff;
  padding: 16rpx 0;
  margin-bottom: 16rpx;
}

.cat-inner {
  display: flex;
  gap: 16rpx;
  padding: 0 24rpx;
}

.cat-tag {
  flex-shrink: 0;
  padding: 10rpx 28rpx;
  border-radius: 100rpx;
  font-size: 26rpx;
  background: #f5f5f5;
  color: var(--text-sub);
}

.cat-tag.cat-active {
  background: var(--primary-light);
  color: var(--primary);
  font-weight: 600;
}

/* 课程列表 */
.course-list {
  padding: 0 24rpx;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

/* 课程统计条 */
.stats-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16rpx 24rpx;
  background: #fff;
  margin: 0 24rpx 16rpx;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,.04);
}
.stats-bar-left { display: flex; align-items: center; gap: 10rpx; }
.stats-bar-icon { font-size: 28rpx; }
.stats-bar-text { font-size: 26rpx; color: #333; font-weight: 600; }
.stats-bar-count {
  font-size: 24rpx; color: #888;
  background: #f5f5f5;
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
}

/* 空状态 */
.empty-state {
  text-align: center;
  padding: 80rpx 40rpx 60rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16rpx;
}
.empty-illust {
  font-size: 100rpx;
  margin-bottom: 8rpx;
}
.empty-title {
  font-size: 30rpx; font-weight: 700; color: #333;
}
.empty-desc {
  font-size: 26rpx; color: #999; margin-top: 4rpx;
}
.empty-action {
  margin-top: 16rpx;
  font-size: 26rpx; color: #fff;
  background: linear-gradient(135deg, #FF6B8A, #6C63FF);
  padding: 16rpx 40rpx;
  border-radius: 40rpx;
  box-shadow: 0 4rpx 16rpx rgba(255,107,138,.3);
}

/* 课程项 */
.item-footer { display: flex; gap: 16rpx; align-items: center; }
.item-learned-tag {
  font-size: 20rpx; color: #fff;
  background: linear-gradient(135deg, #22c55e, #16a34a);
  padding: 2rpx 12rpx; border-radius: 10rpx;
  margin-left: auto;
}

.course-item {
  display: flex;
  overflow: hidden;
}

.item-cover {
  width: 160rpx;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  min-height: 160rpx;
}

.item-emoji {
  font-size: 56rpx;
}

.free-badge {
  position: absolute;
  top: 8rpx;
  left: 8rpx;
  background: #22c55e;
  color: #fff;
  font-size: 18rpx;
  padding: 2rpx 10rpx;
  border-radius: 100rpx;
}

.done-badge {
  position: absolute;
  top: 8rpx;
  right: 8rpx;
  background: rgba(0,0,0,0.4);
  color: #fff;
  font-size: 20rpx;
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.item-info {
  flex: 1;
  padding: 20rpx;
  display: flex;
  flex-direction: column;
  gap: 10rpx;
}

.item-top {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.item-gender-tag {
  font-size: 22rpx;
}

.item-title {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text-main);
  line-height: 1.4;
}

.item-desc {
  font-size: 24rpx;
  color: var(--text-sub);
  line-height: 1.5;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  overflow: hidden;
}

.item-footer {
  display: flex;
  gap: 20rpx;
}

.item-duration, .item-views {
  font-size: 22rpx;
  color: var(--text-hint);
}

/* ===== AI 课程生成 Banner ===== */
.ai-course-banner {
  margin: 0 24rpx 24rpx;
  background: linear-gradient(135deg, #1a1a2e, #16213e);
  border-radius: 20rpx;
  padding: 24rpx 28rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.acb-left { display: flex; align-items: center; gap: 16rpx; flex: 1; }
.acb-icon { font-size: 44rpx; }
.acb-title { font-size: 28rpx; font-weight: 700; color: #fff; margin-bottom: 6rpx; }
.acb-sub { font-size: 22rpx; color: rgba(255,255,255,.6); }
.acb-btn {
  background: linear-gradient(135deg, #FF6B8A, #6C63FF);
  color: #fff;
  font-size: 26rpx;
  font-weight: 700;
  padding: 14rpx 28rpx;
  border-radius: 30rpx;
  min-width: 90rpx;
  text-align: center;
}
.acb-loading { opacity: .6; }

/* ===== AI 课程区 ===== */
.ai-section { margin: 0 24rpx 20rpx; }
.ai-section-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; }
.ai-section-title { font-size: 28rpx; font-weight: 700; color: #6C63FF; }
.ai-section-clear { font-size: 24rpx; color: #999; }
.ai-course-card { border: 1rpx solid #6C63FF22; }
.tag-ai {
  background: linear-gradient(135deg, #4F8EF722, #6C63FF22);
  color: #6C63FF;
  font-size: 20rpx;
  padding: 3rpx 12rpx;
  border-radius: 8rpx;
}

/* ===== 热门标签 ===== */
.hot-badge {
  position: absolute;
  top: 8rpx;
  right: 8rpx;
  font-size: 22rpx;
  background: rgba(0,0,0,.3);
  padding: 2rpx 8rpx;
  border-radius: 10rpx;
}

/* ===== 标题行 ===== */
.section-title-row {
  padding: 0 24rpx 12rpx;
}
.section-ttl-txt {
  font-size: 28rpx;
  font-weight: 700;
  color: #1a1a2e;
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.count-badge {
  display: inline-block;
  background: var(--primary-light, #ffe4ec);
  color: var(--primary, #FF6B8A);
  font-size: 22rpx;
  font-weight: 600;
  padding: 2rpx 14rpx;
  border-radius: 20rpx;
}

.empty {
  text-align: center;
  padding: 80rpx 40rpx;
}
.empty-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
}
.empty-text {
  font-size: 28rpx;
  color: #999;
  display: block;
  margin-bottom: 20rpx;
}
.empty-hint {
  display: inline-block;
  background: var(--primary-light, #ffe4ec);
  color: var(--primary, #FF6B8A);
  font-size: 26rpx;
  padding: 12rpx 32rpx;
  border-radius: 30rpx;
}
