# 2026-04-03 工作日志

## 恋爱进化论小程序 - 灵魂伴侣功能优化

### 完成的功能
1. **预览/清晰同一张图**：付费前后使用同一张图片，预览时添加模糊效果，付费后移除模糊显示清晰版
2. **重新测算需付费**：重新测算按钮只在用户付费后才显示
3. **模糊效果增强**：将预览图片的模糊程度从 15px 提升到 25px，同时调暗亮度
4. **AI 生图支持**：在 utils/ai.js 中添加了 generateSoulmateImage 函数，为后续接入 DeepSeek 生图 API 做准备

### 修复：图片无法显示问题
- **问题原因**：外部图片服务(picsum.photos)在微信小程序中无法加载
- **解决方案**：改用本地CSS渐变生成头像，完全无需外部图片
- **实现方式**：
  - 添加 `generateAvatarColors()` 函数，根据生日生成唯一颜色
  - 使用 `linear-gradient` 背景色 + 抽象的发型/脸部色块
  - 预览时添加 25px 模糊效果，付费后移除模糊显示清晰版

### 修改的文件
- `utils/ai.js` - 添加生图函数
- `pages/soulmate/soulmate.js` - 逻辑调整，添加本地头像生成和API调用
- `pages/soulmate/soulmate.wxml` - 重新测算按钮条件修改，使用渐变头像/图片切换
- `pages/soulmate/soulmate.wxss` - 模糊效果增强，添加渐变头像样式

### 新增：生图云函数
- 创建 `cloudfunctions/generate-image/` 云函数
- 云函数调用逻辑已在 `soulmate.js` 中实现
- 需要在微信开发者工具中上传部署

### 注意事项
- 当前使用本地渐变生成头像，无需网络
- 未来可接入腾讯云AI绘图、阿里通义万相等真正的AI生图服务
- 本地头像暂不支持保存到相册（会提示用户使用截图）

---

## 2026-04-04 更新

### 改用阿里通义万相 API 生图
- **API Key**: `sk-d8f4569e360e46888ab126a1985ff2b0`
- **模型**: `wanx2.1-t2i-turbo`（速度快，效果好）
- **流程**: 异步任务模式 → 提交任务 → 每3秒轮询 → 最多60秒 → 下载保存本地
- **修改文件**: `pages/soulmate/soulmate.js`，替换了 generateAndDownloadImage 函数

### 修复输入框 placeholder 显示不完整
- 微信小程序不支持 CSS `::placeholder` 伪元素，需用 `placeholder-style` 属性
- 修复页面：soulmate.wxml（城市输入）、chat-reply.wxml（ta输入框）、onboarding.wxml（昵称）、roleplay.wxml（输入框）
- 长提示词缩短为精简版避免截断

### 优化微信支付
- 移除测试时多余的 1.2s 延迟，直接进入生图流程
- 增加支付配置注释，方便上线时配置
- `USE_REAL_PAY = false` 测试模式，改为 `true` 配后端 URL 即可
- 网络超时调整为 60秒（适配生图耗时）
